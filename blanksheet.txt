const store = configureStore({
  reducer: {
    [appSlice.reducerPath] : appSlice.reducer
    },
  middlewawre: getDefaultMiddlewawre => 
    getDefaultMiddleware().concat(appSlice.middleware),
  devTools: true
})